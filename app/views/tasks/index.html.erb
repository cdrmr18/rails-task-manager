<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><input type="checkbox"> <%= link_to task.title, task_path(task) %></li> - <%=  link_to '🗑', task_path(task), method: :delete, data: {confirm: "Delete #{task.title}?"} %>
  <% end %>
</ul>

<%= link_to "Add new task", new_task_path %>
